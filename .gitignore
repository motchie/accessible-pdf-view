node_modules/
.output/
.wxt/
stats.html
*.zip

# Generated by scripts/copy-assets.mjs — do not commit vendored binaries.
public/wasm/
public/pdfjs/

# Nothing here ignores working material any more, because none of it is in this
# directory: the test PDFs, the suites written around them, the design files,
# the original brief and the parked notes all live outside the repository. See
# "Kept outside this repository" in README.md.
#
# This one stays as insurance. Windows writes it beside a downloaded file and
# it records where the file came from (`ReferrerUrl=`), so a stray one would
# publish the source of a document this project deliberately does not name.
*:Zone.Identifier
